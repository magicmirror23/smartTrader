# Render config
services:
  - type: web
    name: stocktrader
    runtime: docker
    dockerfilePath: ./Dockerfile
    plan: free
    envVars:
      - key: ALLOWED_ORIGINS
        value: "*"
    healthCheckPath: /api/v1/health
